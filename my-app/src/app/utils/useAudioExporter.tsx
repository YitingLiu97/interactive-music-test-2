"use client";
import { useState, useEffect, useRef } from "react";
import * as Tone from "tone";

export function useAudioExporter(){
    // accepts blob
    // converts to desired format (webm, wav, etc)
    // trigger download and returns download URL 
}